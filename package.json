{
  "name": "@arigato/mamba",
  "version": "0.1.0",
  "description": "A tool for signing URLs using secp521r1",
  "main": "dist/index.js",
  "scripts": {
    "build": "npx tsc src/mamba.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "manifoldco",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^3.1.6"
  }
}
