{
  "name": "@manifoldco/mamba",
  "version": "0.3.6",
  "description": "A tool for signing URLs using secp521r1",
  "main": "dist/mamba.js",
  "types": "dist/mamba.d.ts",
  "repository": "github:manifoldco/mamba",
  "scripts": {
    "build": "npx tsc --declaration",
    "test": "npx jest",
    "test:coverage": "npx jest --coverage",
    "test:debug": "node --inspect-brk node_modules/.bin/jest --runInBand",
    "prepublishOnly": "npm run build"
  },
  "author": "manifoldco",
  "license": "BSD-3-Clause",
  "devDependencies": {
    "@types/jest": "^23.3.10",
    "@types/node": "^14.14.3",
    "jest": "^24.8.0",
    "ts-jest": "^24.0.2",
    "typescript": "^3.1.6"
  },
  "dependencies": {}
}
